import { createFileRoute } from "@tanstack/react-router";
import { ListTree, Clock } from "lucide-react";
import { PageShell } from "@/components/baruna/page/PageShell";
import { trainerPortalNav, EXPERTS_SIDEBAR_META } from "@/data/expertsNav";
import { useTrainerPortal } from "@/lib/experts/useTrainerPortal";
export const Route=createFileRoute("/experts/portal/review-status")({head:()=>({meta:[{title:"Module Review Status — Trainer Portal"}]}),component:ReviewStatusPage});
function ReviewStatusPage(){const q=useTrainerPortal(),drafts=q.data?.moduleDrafts??[];return <PageShell sidebar={{...EXPERTS_SIDEBAR_META,title:"Trainer Portal",subtitle:"Module review pipeline.",sections:trainerPortalNav("/experts/portal/review-status")}} cta={{icon:ListTree,title:"Transparent review history",description:"Decisions and reviewer comments come directly from governance records.",button:"Back to Dashboard",href:"/experts/portal"}}><div className="space-y-6"><div><h1 className="font-display text-3xl font-extrabold text-navy">Module Review Status</h1><p className="mt-2 text-sm text-muted-foreground">Track every module draft and submitted review subject.</p></div>{q.isLoading?<p className="text-sm text-muted-foreground">Loading module reviews…</p>:drafts.length?<div className="space-y-4">{drafts.map(d=><section key={d.id} className="rounded-2xl border border-border bg-card p-6 shadow-soft"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase text-muted-foreground">Module</p><h2 className="mt-1 font-display text-xl font-bold text-navy">{d.title}</h2><p className="mt-1 text-xs text-muted-foreground">Updated {new Date(d.updatedAt).toLocaleDateString()}</p></div><span className="rounded-full bg-marine/10 px-3 py-1 text-xs font-bold capitalize text-marine">{(d.reviewStatus??d.status).replaceAll("_"," ")}</span></div><h3 className="mt-5 font-display text-sm font-bold text-navy">Review History</h3>{d.reviewHistory.length?<ul className="mt-2 divide-y divide-border">{d.reviewHistory.map((h,i)=><li key={`${h.at}-${i}`} className="py-3"><div className="flex justify-between gap-2"><b className="text-sm text-navy">{h.actor}</b><span className="text-xs text-muted-foreground">{new Date(h.at).toLocaleString()}</span></div><p className="mt-1 text-xs font-semibold capitalize text-marine">{h.decision.replaceAll("_"," ")}</p>{h.comment&&<p className="mt-1 text-sm text-foreground/75">{h.comment}</p>}</li>)}</ul>:<p className="mt-2 flex items-center gap-2 rounded-lg bg-muted p-3 text-xs text-muted-foreground"><Clock className="h-4 w-4"/>No reviewer decision has been recorded yet.</p>}</section>)}</div>:<div className="rounded-2xl border border-dashed border-border bg-card p-12 text-center"><ListTree className="mx-auto h-8 w-8 text-marine"/><p className="mt-3 font-display text-lg font-bold text-navy">No module submissions yet</p><p className="mt-1 text-sm text-muted-foreground">Saved drafts and submitted modules will appear here.</p></div>}</div></PageShell>}
